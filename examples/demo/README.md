# Legacy CTA demo

This is **not** the README golden path. `cta-primary-broken` lives here so the fixture oracle and unit tests still have a tiny static app.

Golden path: [`../vite-app`](../vite-app) (Vite + Playwright).

```bash
node examples/demo/server.js
# http://localhost:3000
```
